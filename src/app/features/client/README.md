# Client feature preparation

Task 1 reserves independent layered directories for `personal-data`, `addresses`,
`emails`, and `phone`. Each feature has `application`, `domain`, `infrastructure`,
`presentation`, and `state` folders without implementation logic.

The root `presentation` directory reserves `personal-data-page`,
`contact-data-page`, and `client-overview` composition locations for later tasks.
